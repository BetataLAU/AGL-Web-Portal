node_modules/
.env
npm-debug.log*
.DS_Store
*.log
.project-state.json
__pycache__/
*.pyc
# SQLite database (use npm run db:export / db:import to sync)
database.db
# SQLite session store (connect-sqlite3)
db/sessions.db

# Shipper Role Project - 暫存/產出（不進 repo）
data/work/
data/uploads/

# VS Code（個人設定，不進 repo）
.vscode/
data/test-*

# 模板備份檔（本機保存即可，不進 repo）
data/templates/* (BAK).xlsx

# 研究抓取輸出（可再生成，不進 repo；見 docs/README.md）
docs/research/*-latest.txt
